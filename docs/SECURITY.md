# Security Analysis — Adaptive LP Vault

> 安全风险分析与缓解措施文档

---

## 目录

- [概述](#概述)
- [风险评级说明](#风险评级说明)
- [已识别风险与缓解措施](#已识别风险与缓解措施)
  - [1. 三明治攻击（Sandwich Attack）](#1-三明治攻击sandwich-attack)
  - [2. 重入攻击（Reentrancy）](#2-重入攻击reentrancy)
  - [3. 预言机操纵（Oracle Manipulation）](#3-预言机操纵oracle-manipulation)
  - [4. 无常损失（Impermanent Loss）](#4-无常损失impermanent-loss)
  - [5. 女巫攻击激励（Sybil Attack on Incentives）](#5-女巫攻击激励sybil-attack-on-incentives)
  - [6. 治理参数滥用（Governance Parameter Abuse）](#6-治理参数滥用governance-parameter-abuse)
- [已知限制与改进方向](#已知限制与改进方向)
  - [1. V3 再平衡撤出缺少显式滑点保护](#1-v3-再平衡撤出缺少显式滑点保护)
  - [2. V3 存款传入的 minAmount 为 0](#2-v3-存款传入的-minamount-为-0)
  - [3. 两套滑点参数未同步](#3-两套滑点参数未同步)
  - [4. ERC4626 标准接口无滑点参数](#4-erc4626-标准接口无滑点参数)

---

## 概述

本文档详细分析 Adaptive LP Vault 系统中存在的潜在安全风险，并说明已实施的缓解措施。所有风险均经过攻击场景推演和代码级验证。

系统采用多层安全防护架构，包括但不限于：重入保护、滑点保护、TWAP 预言机、权限管理、参数边界限制等。

---

## 风险评级说明

| 评级 | 含义 |
|------|------|
| 🔴 高 | 可能导致资金直接损失，需重点防护 |
| 🟡 中 | 可能导致收益受损或系统异常，需关注 |
| 🟢 低 | 影响有限，已有基础防护 |

---

## 已识别风险与缓解措施

### 1. 三明治攻击（Sandwich Attack）

**风险评级**：🔴 高

#### 攻击场景

再平衡操作需要在不同流动性场所间转移资金，涉及添加/移除流动性和代币兑换。攻击者可以：

1. 监听内存池（mempool）中的再平衡交易
2. 在再平衡交易前抢先买入，推高目标代币价格
3. 再平衡交易以不利价格执行
4. 攻击者在再平衡交易后卖出，获取利润

这种攻击会导致金库资金以不利价格进行再平衡，损害所有份额持有者的利益。

#### 缓解措施

滑点保护按操作路径覆盖，**用户存取款路径、V2 全路径与 V3 按比例撤出路径设有滑点下限**：

- **用户级滑点保护**：`deposit(weth, usdc, minShares)` 和 `withdrawDual(shares, minWETH, minUSDC)` 要求用户传入最小输出量，防止交易执行时价格偏离预期
- **V2 内部滑点保护**：V2 适配器的 `addLiquidity` 和 `withdrawAll` 内部根据 pair 储备计算滑点下限
- **V3 按比例撤出滑点保护**：Vault 根据当前价格估算预期输出并乘以滑点容忍度，传入 `amount0Min` / `amount1Min`，V3 适配器双重校验
- **TWAP 参考价**：再平衡决策基于 TWAP 时间加权均价，而非瞬时现货价格，降低攻击者操纵参考价的能力
- **激励校验**：再平衡激励发放前校验操作是否产生正向收益，亏损的再平衡不会获得奖励

> ⚠️ **适用边界**：上述滑点保护不覆盖 **V3 再平衡投资（最小输出传 0）与 V3 再平衡全量撤出（`withdrawAll` 无最小输出校验）** 两条路径，该路径存在被三明治攻击的残留风险；缓解依赖冷却期（10/30 分钟）、执行时机不可预测与 TWAP 估值等辅助机制。例外路径详情见 [已知限制与改进方向](#已知限制与改进方向)。

---

### 2. 重入攻击（Reentrancy）

**风险评级**：🔴 高

#### 攻击场景

金库涉及大量代币转账操作（存款、取款、再平衡、领取激励等）。如果某个代币是恶意合约（带有回调函数的 ERC777 或其他非标准代币），攻击者可以：

1. 向金库存款，获得份额
2. 在取款时，恶意代币的 `tokensToSend` 回调中重入金库的 `withdraw` 函数
3. 在状态更新完成前重复取款，盗取超出实际存款的资金

#### 缓解措施

- **ReentrancyGuard**：核心金库 `AdaptiveLPVault` 继承 OpenZeppelin 的 `ReentrancyGuard`，所有涉及外部调用和状态变更的函数均加 `nonReentrant` 修饰符
- **Checks-Effects-Interactions 模式**：所有状态变更（更新份额、记录余额等）均在外部代币转账之前完成
- **标准代币假设**：系统设计假设使用标准 ERC20 代币（WETH、USDC），不支持带有回调的非标准代币
- **SafeERC20**：所有代币操作使用 OpenZeppelin 的 `SafeERC20` 库，安全处理非标准代币的返回值

---

### 3. 预言机操纵（Oracle Manipulation）

**风险评级**：🔴 高

#### 攻击场景

系统的再平衡决策依赖 TWAP 预言机提供的价格数据。如果攻击者能够操纵预言机价格，可以：

1. 通过闪电贷借入大量资金，短期操纵 Uniswap V3 池的现货价格
2. 被操纵的价格被预言机读取，导致错误的再平衡决策
3. 系统以错误的价格区间和资金配比进行再平衡
4. 攻击者利用错误的仓位设置获取利润

#### 缓解措施

- **TWAP 时间加权均价**：使用 Uniswap V3 原生的 TWAP（时间加权平均价格），而非瞬时现货价格。默认采用 30 分钟时间窗口，攻击者需要在整个窗口内维持价格操纵才能影响 TWAP
- **观测窗口可治理**：TWAP 窗口参数可通过治理调整，最小窗口限制防止设置过短的窗口
- **价格偏离校验**：再平衡前校验现货价格与 TWAP 价格的偏离度，偏离过大时暂停再平衡
- **多数据源设计**：架构支持接入多个预言机数据源，当前使用 V3 池 TWAP，未来可扩展接入 Chainlink 等外部预言机

---

### 4. 无常损失（Impermanent Loss）

**风险评级**：🟡 中

#### 攻击场景/风险描述

LP 做市本质上存在无常损失风险。当 WETH/USDC 价格发生大幅波动时：

1. 金库的 LP 头寸会自动再平衡为更多贬值代币、更少升值代币
2. 与简单持有两种代币相比，LP 头寸的价值会更低
3. 如果价格持续单向大幅波动，无常损失可能超过手续费收益
4. 用户赎回时可能获得比存款时更少的本金

这不是"攻击"，而是 AMM 做市的固有风险，但会直接影响用户收益。

#### 缓解措施

- **波动率自适应策略**：根据市场波动率动态调整资金配比。高波动时增加 V2 配比（V2 是无限区间，无常损失相对较小），低波动时集中在 V3 高费率池（最大化手续费收益）
- **三层区间做市**：V3 资金分散在窄/中/宽三个价格区间，避免所有资金集中在一个区间。即使价格突破窄区间，中/宽区间仍在赚取手续费
- **动态区间调整**：再平衡时根据当前 TWAP 价格重新设置三层区间的中心价格，确保区间围绕当前价格
- **V2 对冲**：部分资金配置在 Uniswap V2（恒定乘积做市，无限区间），作为 V3 集中流动性的对冲
- **手续费收益累积**：做市手续费自动累积到金库资产中，部分抵消无常损失

---

### 5. 女巫攻击激励（Sybil Attack on Incentives）

**风险评级**：🟡 中

#### 攻击场景

系统设计了去中心化再平衡激励机制，任何人触发再平衡并产生正向收益时，可获得 USDC 奖励。攻击者可能：

1. 创建大量地址（女巫攻击）
2. 用每个地址反复触发小额再平衡
3. 每次再平衡产生微小正向收益，从而累积大量激励奖励
4. 激励资金被攻击者榨取，损害真正的再平衡执行者利益

#### 缓解措施

- **正向收益校验**：激励发放前严格校验再平衡操作是否产生正向收益（金库资产净值增加），亏损或零收益的再平衡不发放奖励
- **最小收益阈值**：设置最小收益阈值，只有再平衡收益超过阈值时才发放激励，防止小额反复操作
- **冷却期**：同一地址触发再平衡后有冷却期，冷却期内再次触发不获得奖励
- **激励比例上限**：激励金额为再平衡收益的固定比例（可治理调整），且有单次上限，防止单次操作榨取大量激励
- **激励资金独立管理**：激励资金存放在独立的 `RebalanceIncentives` 合约中，与金库资产隔离，即使激励被榨取也不影响金库本金

---

## 已知限制与改进方向

本节记录系统当前存在的已知安全限制，以及对应的改进方向。这些限制不影响核心功能的正常运行，但在极端市场条件或特定攻击场景下可能构成风险。

### 1. V3 再平衡撤出缺少显式滑点保护

**风险评级**：🟡 中

> 本节是 [风险 1：三明治攻击](#1-三明治攻击sandwich-attack) 中"适用边界"所指向的例外路径详情。

#### 问题描述

`UniswapV3Adapter.withdrawAll()` 在再平衡流程中被调用，用于从所有 V3 仓位撤出流动性。该函数直接调用 `POOL.burn(tickLower, tickUpper, liquidity)` 和 `POOL.collect(...)`，未传入 `amount0Min` / `amount1Min` 参数，也未在函数内部做输出量的下限校验。

相比之下，V2 路径的 `withdrawAll()` 会根据 pair 储备量计算预期输出，并乘以 `(10000 - maxSlippageBps) / 10000` 作为滑点下限。

#### 潜在攻击场景

理论上存在被三明治攻击的风险：

1. 攻击者监听内存池中的 `rebalance()` 交易
2. 在再平衡交易执行前，在 V3 池中大额 swap 推偏现货价格
3. 再平衡交易执行，`withdrawAll()` 按被操纵的价格撤出流动性，金库获得的代币数量偏离公允价格
4. 攻击者反向 swap 平仓获利

#### 当前缓解措施

- **再平衡冷却期**：正常情况下 10 分钟冷却期，高波动率（>50%）时 30 分钟紧急冷却期，降低被频繁攻击的频率
- **执行时间不可预测**：再平衡由外部激励触发，任何人可调用，执行时间不固定，增加攻击者提前布局的难度
- **V2 路径有保护**：V2 适配器的 `withdrawAll()` 有完整的滑点下限计算
- **TWAP 估值**：金库总资产估值使用 TWAP 价格而非现货价格，短期价格操纵不影响净值计算

---

### 2. V3 存款传入的 minAmount 为 0

**风险评级**：🟢 低

#### 问题描述

`AdaptiveLPVault._investOneRange()` 调用 V3 适配器的 `addLiquidity()` 时，传入的 `amount0Min` 和 `amount1Min` 均为 `0`。

V3 适配器内部虽然有 `require(amount0 >= amount0Min && amount1 >= amount1Min, "V3Adapter: slippage")` 检查，但由于传入的最小值为 0，该检查永远通过，形同虚设。

#### 影响分析

V3 `mint` 是按预先计算好的 `liquidity` 数量存入，实际消耗的 token0/token1 数量由池内当前价格决定。价格偏离的影响主要体现在：

- 实际存入量与预期存入量的差异（dust）
- 多余的未使用资金会留在适配器中，随后通过 `_returnDust()` 退回金库

因此该问题不会导致资金损失，只会导致资金利用效率略有降低。

---

### 3. 两套滑点参数未同步

**风险评级**：🟡 中

#### 问题描述

系统中存在多套独立的滑点参数，未统一管理：

- **金库层**：`AdaptiveLPVault.maxSlippageBps`（默认 1%），用于用户取款时的最小输出量校验
- **V2 适配器层**：`UniswapV2Adapter.maxSlippageBps`（默认 1%），用于 V2 添加/移除流动性时的滑点下限计算
- **V3 适配器层**：无滑点参数，`addLiquidity` 和 `withdrawAll` 均未设置输出量下限

三套参数各自独立，修改金库的 `maxSlippageBps` 不会同步到 V2 适配器，V3 适配器则完全没有滑点保护。

#### 影响分析

- 不同流动性路径的滑点保护标准不一致，用户无法通过单一参数统一控制风险
- V3 路径在极端行情下缺乏滑点保护，可能遭受三明治攻击（详见第1条）
- 治理修改金库滑点参数后，V2 适配器仍使用旧值，造成预期与实际不符

#### 改进方向

- 统一滑点参数来源：V2/V3 适配器均读取金库的 `maxSlippageBps`，不再各自维护
- 为 V3 适配器的 `withdrawAll` 和 `addLiquidity` 增加基于 TWAP 参考价的输出量下限校验
- 治理修改滑点参数时，通过事件通知所有适配器同步更新

---

### 4. ERC4626 标准接口无滑点参数

**风险评级**：🟢 低

#### 问题描述

ERC4626 标准接口定义的 `deposit(assets, receiver)`、`mint(shares, receiver)`、`withdraw(assets, receiver, owner)`、`redeem(shares, receiver, owner)` 均不包含滑点保护参数。用户通过标准 ERC4626 接口存款时，无法指定最小获得份额；取款时无法指定最小获得资产量。

在价格剧烈波动的场景下，用户提交交易到交易上链的时间差内，金库净值可能发生变化，导致用户实际获得的份额/资产低于预期。

#### 当前缓解措施

- **自定义双币存款接口**：提供 `deposit(uint256 wethAmount, uint256 usdcAmount, uint256 minShares)`，支持用户指定最小获得份额
- **自定义双币取款接口**：提供 `withdrawDual(uint256 shares, uint256 minWETH, uint256 minUSDC)`，支持用户指定最小获得的 WETH 和 USDC 数量
- **前端引导**：前端默认使用自定义接口，引导用户设置合理的滑点容忍度
- **TWAP 估值**：金库净值使用 TWAP 价格计算，短期价格波动对净值影响有限

#### 改进方向

- 在 ERC4626 标准接口的实现中增加内部滑点校验（基于全局 `maxSlippageBps`）
- 前端明确标注标准接口与自定义接口的区别，引导用户使用带滑点保护的自定义接口
- 考虑实现 ERC-7540（异步金库）标准，将存款/取款拆分为请求和结算两步，从根本上消除时间差风险

---

### 5. 单币存款无法参与做市

**风险评级**：🟡 中

#### 问题描述

Uniswap V2 和 V3 的流动性添加均要求同时提供两种代币：

- **V2**：`addLiquidity` 必须同时传入 token0 和 token1 的数量，且按池内储备比例自动调整实际存入量
- **V3 区间内**：当现货价格处于做市区间内时，`mint` 需要同时消耗 token0 和 token1；仅当价格在区间外时才单边收取一种代币

本系统的 V3 做市区间围绕当前 TWAP 价格设置（±2%、±10%、±30%），当前价格必然处于最宽区间内，因此再平衡投资时需要同时消耗 WETH 和 USDC。

如果用户仅存入单一代币（如仅 USDC），金库合约中另一种代币（WETH）余额为0，再平衡时：
- V2 路径因缺少 WETH 跳过投资（代码中 `wethForV2 > WETH_DUST_THRESHOLD && usdcForV2 > USDC_DUST_THRESHOLD` 检查）
- V3 路径因 `_calculateLiquidity` 计算出的流动性为0（仅单币无法在区间内提供流动性），`addLiquidity` 直接返回不投资

最终资金全部停留在金库合约的"闲置"区域，无法参与做市赚取手续费。

#### 影响分析

- **资金利用率为0**：存入的单币资金完全不产生做市手续费收益
- **用户体验差**：用户存款后触发再平衡，资金分布无变化，可能误以为系统故障
- **再平衡无意义**：闲置资金不参与做市，再平衡操作仅产生 gas 费消耗，无实际收益

#### 当前缓解措施

- **前端双币存款界面**：前端同时提供 WETH 和 USDC 两个输入框，并在存款页面显著位置提示用户必须同时存入两种代币
- **V3 单边容忍**：V3 适配器的 `_investOneRange` 使用 `||` 检查（`wethAmt > threshold || usdcAmt > threshold`），理论上支持单边投资，但在区间内场景下实际流动性为0
- **dust 阈值保护**：设置最小操作门槛，避免极小额资金因比例计算问题导致交易失败

#### 改进方向

- **合约层自动换币**：在 `_investIdleFunds` 和 `_reinvestWithRanges` 中，当仅有单币时，通过 Uniswap 路由将约一半价值的代币兑换为另一种，再进行做市
- **前端强制校验**：存款时校验 WETH 和 USDC 数量均大于0，否则禁止提交并提示用户
- **闲置资金生息**：将闲置资金存入 Aave/Compound 等借贷协议赚取利息，提高资金利用率
- **文档明确说明**：在 README 和前端页面明确说明双币存款要求，避免用户误解

---

## License

MIT License
